#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
int r ;
float n=3.14,cap,cevre,alan;
printf ("cemberin yaricapi=");
scanf ("%d",&r);
cap=r*2;
cevre=2*n*r;
alan=4*n*r*r;
printf("cembrin capi=%.6f\ncemberin cevresi=%.6f\ncemberin alani=%.6f\n",cap,cevre,alan);
	return 0;
}
